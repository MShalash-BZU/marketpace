name: marketplace_app
description: Multi-store marketplace delivery platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Authentication
  supabase_flutter: ^2.7.0
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.0
  
  # Phone Auth
  pinput: ^5.0.0  # لإدخال OTP
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # UI
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  flutter_localizations:
    sdk: flutter

  # State Management
  riverpod_annotation: ^2.3.3

  # Backend & API
  dio: ^5.4.0
  
  # Functional Programming
  dartz: ^0.10.1
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  
  # Navigation
  go_router: ^12.1.3
  
  # UI Components
  shimmer: ^3.0.0
  flutter_rating_bar: ^4.0.1
  badges: ^3.1.2
  
  # Forms & Validation
  flutter_form_builder: ^9.1.1
  form_builder_validators: ^9.1.0
  
  # Utils
  intl: ^0.20.2
  uuid: ^4.2.1
  equatable: ^2.0.5
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  
  # Image Handling
  image_picker: ^1.0.5
  image: ^4.1.3
  
  # Notifications
  firebase_core: ^4.3.0
  firebase_messaging: ^16.1.0
  flutter_local_notifications: ^19.5.0
  
  # QR Code
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.5
  
  # Connectivity
  connectivity_plus: ^5.0.2
  
  # Permissions
  permission_handler: ^11.1.0
  
  # Error Tracking
  # sentry_flutter: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  
  # Code Generation
  build_runner: ^2.4.7
  riverpod_generator: ^2.3.9
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  hive_generator: ^2.0.1

dependency_overrides:
  intl: ^0.20.2

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/

